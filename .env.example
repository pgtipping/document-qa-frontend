# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8001

NEXT_PUBLIC_BASE_URL=http://localhost:3004
# Sentry Configuration (Optional)
NEXT_PUBLIC_SENTRY_DSN=

# Email Alert Configuration
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
EMAIL_RECIPIENT=recipient@example.com

# Analytics ID (Optional)
NEXT_PUBLIC_GA_ID=

# Feature Flags
ENABLE_METRICS_DASHBOARD=true
ENABLE_MODEL_SELECTION=true

# Admin Access
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your-secure-admin-password  # Must be at least 8 characters with uppercase, lowercase, number, and special character

# JWT Configuration
JWT_SECRET=your-secure-jwt-secret  # Use a strong random string like the one generated with crypto.randomBytes(64).toString('base64')

# Note: For the SENDING Gmail account (EMAIL_USER):
# 1. Enable 2-Step Verification in that Gmail account
# 2. Go to Security > App Passwords
# 3. Generate a new App Password for 'Mail'
# 4. Use that password as EMAIL_PASSWORD

# Analytics (Optional)
NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id

# Feature Flags
NEXT_PUBLIC_ENABLE_METRICS_DASHBOARD=true
NEXT_PUBLIC_ENABLE_MODEL_SELECTION=true 

# Admin Access
ADMIN_KEY=your-secure-admin-key  # Use a strong, random string for production 

# SMTP Configuration
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASSWORD=your_sendgrid_api_key_here
SMTP_FROM_EMAIL=info@document-qa.com
SMTP_FROM_NAME=Document Q&A

# Contact Form
CONTACT_FORM_TO_EMAIL=pgtipping1@gmail.com

# The following variables should be set in Vercel environment settings:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=your_sendgrid_api_key_here
# SMTP_FROM_EMAIL=info@document-qa.com
# SMTP_FROM_NAME=Document Q&A
# CONTACT_FORM_TO_EMAIL=pgtipping1@gmail.com 
# NextAuth.js Configuration
# Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-here
NEXTAUTH_URL=http://localhost:3004 # Or your deployment URL

# GitHub OAuth Provider (Optional)
GITHUB_ID=your-github-client-id
GITHUB_SECRET=your-github-client-secret